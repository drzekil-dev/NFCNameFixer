# 한글 파일명 변환기 — 사용 설명서

맥에서 만든 한글 파일·폴더 이름을 Windows 호환(NFC)으로 바꿔주는 메뉴바 앱입니다. (v1.0.1)

---

## 1. 설치 & 실행

1. `NFCNameFixer.app`을 더블클릭 → 상단 **메뉴바에 아이콘**(`텍스트` 모양)이 뜹니다. (Dock 아이콘은 없습니다.)
2. 메뉴바 아이콘을 **클릭하면 창**이 열립니다.

> 첫 실행 때 "확인되지 않은 개발자" 경고가 뜨면, `NFCNameFixer.app`을 **우클릭 → 열기**를 한 번 해주세요.

---

## 2. 맥 → 윈도 (NFC 변환)

> 맥에서 만든 한글 파일을 **메일·압축으로 윈도에 보낼 때 이름이 깨지지 않게** 합니다.

- **바로 변환**: 폴더나 파일을 창에 **끌어다 놓기** → 즉시 NFC로 변환 (하위 폴더까지)
- **자동 감시**: 켜 두면, 등록한 폴더에 새 한글 파일이 생길 때마다 **자동 변환**
  - **감시 폴더 추가…** 버튼으로 폴더 등록 (또는 폴더를 드롭하면 자동 등록)
  - **지정 폴더 지금 스캔**: 감시 폴더 전체를 즉시 한 번 점검
- **로그인 시 시작**: 켜 두면 로그인할 때 앱이 자동 실행

---

## 3. 업데이트 확인

- 창 하단에 **현재 버전**(v1.0.1)이 표시됩니다.
- **업데이트 확인** 버튼을 누르면 GitHub 최신 릴리스와 비교해, 새 버전이 있으면 **다운로드 페이지**를 열어줍니다.

> 참고: "윈도 → 맥 깨짐 복원" 기능은 코드에 있으나 이 버전에서는 비활성(주석 처리)입니다.

---

## 4. 윈도로 보낼 때 — 첨부·압축 주의 (중요)

NFC로 고쳐도 **보내는 방법**에 따라 윈도에서 다시 깨질 수 있습니다. 원인이 두 가지라서요.

- **(1) 자모 분리(NFD)** → 이 앱이 NFC로 해결
- **(2) 압축 플래그 문제** → macOS 기본 압축(Finder "압축"/`zip`)은 파일명을 UTF-8로 저장하면서 **"UTF-8 플래그"를 안 켜서**, 윈도 탐색기가 한글 바이트를 CP949로 오해석해 깨뜨립니다.

### 권장 방법

| 방법 | 한글 파일명 |
|---|---|
| **맥 "메일" 앱에서 직접 첨부** (압축 X) | ✅ 안전 — NFC 유지 |
| **Gmail 웹 — Safari** 로 첨부 | ✅ 안전 — Safari는 NFC 유지 |
| **반디집으로 zip → 첨부** (웹/메일 무관) | ✅ 안전 — 압축 *안*의 이름 보존 |
| **Gmail 웹 — Chrome** 으로 첨부 | ❌ 주의 — Chrome이 NFC를 **NFD로 재분해** |
| macOS Finder "압축" → 첨부 | ❌ 깨짐 (UTF-8 플래그 없음) |

> **두 가지 함정이 있습니다.**
> 1. **압축**: macOS 기본 "압축"은 UTF-8 플래그를 안 켜 윈도에서 깨짐 → **반디집**을 쓰세요.
> 2. **브라우저 업로드**: 같은 Gmail 웹이라도 **Safari는 NFC 유지 / Chrome은 NFD로 되돌림**(둘 다 실측 확인). Chrome으로 첨부할 거면 **반디집 zip으로 묶어** 올리세요(압축 안의 이름은 브라우저가 못 건드림).
>
> 가장 안전: **이 앱으로 NFC 변환 → 맥 "메일" 앱 또는 Safari Gmail로 첨부.**
> (진단 도구: `tools/chrome-nfc-test.html`를 어떤 브라우저로든 열어 파일을 드래그하면, 그 브라우저가 NFC를 유지하는지 즉석에서 확인할 수 있습니다.)

---

## 5. 종료 / 창 닫기

| 동작 | 결과 |
|---|---|
| 창의 **"종료"** 버튼 | 앱 완전 종료 |
| **Cmd-Q** / 창 빨간 닫기 버튼 | 창만 숨김 (메뉴바에는 계속 상주) |
| 메뉴바 아이콘 **우클릭 → 종료** | 앱 완전 종료 |

---

## 6. 자주 묻는 질문

**Q. 변환됐는지 어떻게 확인하나요?**
맥의 `ls`·Finder로는 NFD/NFC 구분이 안 보입니다. 터미널에서 `./check.sh "/폴더경로"`를 실행하면 `남은 NFD: 0개 ✅`로 확인됩니다.

**Q. 감시 폴더를 켰는데 자동 변환이 안 돼요.**
`~/Downloads`·`~/Desktop`·`~/Documents` 같은 폴더는 처음에 macOS가 **접근 허용 창**을 띄웁니다 → "허용"을 누른 뒤, 창을 닫았다 다시 열면 정상 동작합니다.

**Q. 이름에 `?`(물음표)가 박힌 파일은 복원이 안 돼요.**
그건 원래 글자가 `?`로 **치환되어 사라진** 경우라 어떤 도구로도 복구할 수 없습니다. 보낸 사람에게 다시 받거나, 웹 메일에서 직접 내려받으세요.

**Q. 로그인 자동 시작이 잘 안 잡혀요.**
앱을 **`/응용 프로그램(Applications)`** 폴더로 옮긴 뒤 "로그인 시 시작"을 켜는 것이 가장 안정적입니다. (시스템 설정 → 일반 → 로그인 항목에서 확인)
